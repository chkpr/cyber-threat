import { Routes } from '@angular/router';
import { DigestPageComponent } from './features/digest/digest-page.component';


export const routes: Routes = [
  { path: '', component: DigestPageComponent },
];
